{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "ingestion/Dockerfile"
  },
  "deploy": {
    "startCommand": "python init_db.py && python seed_standards.py && python seed_qco.py && python seed_verification.py && python embed_corpus.py && python seed_demo_users.py",
    "restartPolicyType": "NEVER"
  }
}
